## rules

1. scrapingは(直列,2秒ディレイ,browser serviceを使う)のデフォルトから変えないこと. 時間がかかってもscrapingのマナーを守ること.
2. fall backは問題が発覚しづらくなるから禁止.
   どうしても実装すべきだと思う場合はuserの許可を取ること.
3. 検証用ヘルパーやデータ読込は単一の正規ソースを前提に保つこと.
   複数ソースの横断や代替探索が必要ならuserに確認すること.
